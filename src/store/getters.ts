
const getters = {
  // // wechat
  // openid: state => {
  //   if (state.app.openid) return state.app.openid;
  //   return Vue.$cookies.get('openid')
  // },
  // wechatInfo: state => {
  //   if (state.app.wechatInfo) return state.app.wechatInfo;
  //   return JSON.parse(localStorage.getItem('wechatInfo'))
  // },
  // redirect: state => {
  //   if (state.app.redirect) return state.app.redirect
  //   return Vue.$cookies.get('redirect')
  // },
  // // vuex test
  // count: state => state.vuex.count, // es6 中单行代码可省略 {} 以及 return
  // cartList: state => state.vuex.cartList
}
export default getters